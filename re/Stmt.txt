program main;
const
    a = 10;
var
    b,c;
    procedure p;
    begin
        c := b + a
    end;
begin
    read(b,b,b,b);
    while b < 0 do
    begin
        call p;
        write(2 * c,b,b,b);
        read(b)
    end
end